// const { app, mock, assert } = require('egg-mock/bootstrap');

// describe('test app/extend/request.js', () => {

//     describe('set responseTime()', () => {

//         it('should return response time', () => {
//             const ctx = app.mockContext();
//             responseTime = Date.parse(new Date());
//             ctx.response.set() = responseTime;
//             assert.strictEqual(ctx.response.responseTime, responseTime, `response's response time should can be set`);
//         });
//     });
// });